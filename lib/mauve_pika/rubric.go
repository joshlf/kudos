package mauve_pika

// Kudos supports interactive grading in which a rubric, either on the handin
// level or on the assignment level, meaning that kudos will extract a given
// handin and drop a TA into an environment with the contents of the handin
// along with a rubric generated from the assignment config. A TA will then
// fill out this config manually. Kudos will validate this and input the grade
// into the database the database if it passes validation.
// There are currently only stubs in here. There's an argument to be made that
// this can go in an entirely separate package

type Rubric struct{}
